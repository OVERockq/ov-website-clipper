version: '3'

services:
  web:
    build: ./app
    ports:
      - "5005:5001"
    volumes:
      - ./app/templates:/app/templates
      - ./uploads:/tmp/uploads
    environment:
      - PYTHONUNBUFFERED=1
      - DISABLE_TRANSLATION=true
    restart: unless-stopped
